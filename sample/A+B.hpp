#pragma once
#include <cstdint>

/**
 * @brief returns the sum of two integers
 * @param a is the first integer and b is the second one
 * @return sum of a and b
 */
int64_t sum(int64_t a, int64_t b) { return a + b; }
